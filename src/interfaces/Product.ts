export interface ProductDetails {
    creationDate: string,
    currentPrice?: number,
    description?: string,
    fullPrice: number,
    imageSrc?: string,
    quantity: number,
    title: string
}