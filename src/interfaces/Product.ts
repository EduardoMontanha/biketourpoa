export interface ProductDetails {
    creationDate: string,
    date?: string,
    description?: string,
    fullPrice: number,
    id: number,
    imageSrc?: string,
    location: string,
    nextDates?: string[],
    quantity: number,
    salePrice?: number,
    title: string
}