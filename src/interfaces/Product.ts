export interface ProductDetails {
    creationDate: string,
    date?: string,
    description?: string,
    id: number,
    imageSrc?: string,
    location: string,
    nextDates?: string[],
    quantity: number,
    price: number,
    salePrice?: number,
    title: string
}