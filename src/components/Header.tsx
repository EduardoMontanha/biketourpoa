import { HeaderArea } from "../styles/components/Header";

const Header = () => {

    return (
        <HeaderArea>
            <div className="container">
                header
            </div>
        </HeaderArea>
    );
};

export default Header;